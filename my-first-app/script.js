"use strict";
const message = "";

const d = document;
const startButton = d.querySelector(".button-start");
const titleContainer = d.querySelector(".title-container");
const formContainer = d.querySelector(".form-container");
const displayContainer = d.querySelector(".display-container");

window.onload = () => {
  titleContainer.style.opacity = "1";
};

startButton.addEventListener("click", () => {});
